# Review Endroit

1. Compare behavior with `README.md`, `docs/architecture.md`,
   `docs/lifecycles.md`, `docs/reference.md` and the JSON schemas.
2. Verify that the Kernel owns grammar, composition, source lifecycle,
   projection safety and trust—never first-party product behavior.
3. Verify that Equipment own their capabilities, surfaces and runtimes.
4. Reject hidden package-manager behavior, implicit persistence and execution
   during Equipment add, sync, build, doctor or resolution.
5. Run the smallest relevant checks, then the complete qualification matrix for
   a release candidate.
