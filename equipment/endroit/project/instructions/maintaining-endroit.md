Endroit is maintained as an independent Site linked to a Home. Never turn the
product repository into a colocated Home.

Change canonical sources, `equipment.json` manifests and bundled Equipment; never edit
provider projections as source. Keep README, architecture, lifecycle, reference,
schemas, CLI and tests aligned. Sites remain independent Git repositories.

Preserve the user's language. Persist no transcript, hidden reasoning,
credential or private downstream information. Before claiming completion, run
the Node 22/24, check, test, conformance, provider, pack and lab gates.
