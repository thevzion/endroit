Review the current Hairness change from public contract to evidence.

1. Compare behavior with README, reference documentation and JSON schemas.
2. Confirm that Home remains primary, Assets stay source-owned and Targets stay
   independent.
3. Reject hidden package-manager behavior, implicit persistence and
   install-time execution.
4. Run the smallest relevant checks, then the full release matrix.
5. Report observed failures without inferring remote health.
