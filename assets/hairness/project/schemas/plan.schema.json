{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "type": "object",
  "required": ["id", "kind", "owner", "state", "createdBy", "createdAt"],
  "properties": {
    "kind": { "const": "hairness/project:plan" },
    "state": { "enum": ["draft", "accepted", "qualified"] }
  }
}
