# Planning to tickets

Create or update `TICKETS.md` with stable IDs `T001`, `T002` and so on.

Each ticket records status, dependencies, bounded task and acceptance criteria.
Never renumber completed tickets or create a second ticket file.
