# Scratch

Use Scratch for uncertain, non-trivial or resumable work. Create it with:

```bash
hairness artifact create hairness/scratch:scratch <slug> --workspace desk/<workspace>
```

Write only useful state, decisions, evidence, open questions and the next
handoff. Never persist a transcript or hidden reasoning. Promote durable
results explicitly with `artifact promote`.
