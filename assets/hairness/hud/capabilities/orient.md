# Orient in the Home

Use `hairness hud --prompt` when no HUD was injected for this session. Use
`hairness hud --full` for a complete inventory and `hairness hud --json` when
stable data is needed.

Treat HUD evidence as a local snapshot. Revalidate a Target immediately before
mutating it.
