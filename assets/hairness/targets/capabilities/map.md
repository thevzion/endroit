# Map a Target

Use `hairness target map <id> [--binding <id>]`. Read tracked files and bounded
package metadata, cite the Target HEAD and mapping time, expose uncertainty and
write only a `hairness/targets:target-map` Artifact in the Desk.

Never write mapping files into the Target.
