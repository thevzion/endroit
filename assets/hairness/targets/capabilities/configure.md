# Configure Targets

Inspect the current HUD and ask which repositories matter to this Home. Propose
shared Target declarations separately from personal Bindings. Never persist a
machine path in Home or Desk JSON.

Use `hairness target discover`, then present exact additions and bindings before
requesting consent. Offer a Target Map only after a Binding is usable.
